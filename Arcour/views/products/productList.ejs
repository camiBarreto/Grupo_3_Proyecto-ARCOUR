<!DOCTYPE html>
<html lang="en">

<head>
    <%- include("../partials/head.ejs") %>
</head>

<header>
    <%- include("../partials/header.ejs") %>
</header>

<nav>
    <%- include("../partials/navBar.ejs") %>
</nav>

<body>
    <div class="flight-list">
        <% for (const flight of flights) { %>
            <div class="flight-item">
                <div class="flight-number">Vuelo #<%= flight.flightNumber %>
                </div>
                <div class="titles">Salida: <span class="departure-date-badge">
                        <%= flight.departureDate %>
                    </span>
                    <%= flight.departureTime %>
                </div>
                <div class="titles">Llegada: <%= flight.arrivalDate %>
                        <%= flight.arrivalTime %>
                </div>
                <div class="titles">Duración: <%= flight.flightDuration %>
                </div>
                <div class="titles">Aeropuerto de salida: <%= flight.departureAirport %>
                </div>
                <div class="titles">Aeropuerto de llegada: <%= flight.arrivalAirport %>
                </div>
                <div class="info-container">
                    <div class="flight-price">
                        Precio: $<%= flight.ticketPrice %>
                    </div>
                </div>
                <div class="buttons">
                    <div class="button-container">
                        <a href="/products/<%= flight.flightNumber %>/productEdits">
                            <button class="edit-button">Editar</button>
                        </a>

                        <form action="/products/<%= flight.flight_number %>/delete?_method=DELETE" method="post">
                            <input class="delete-button" type="submit" value="Eliminar">
                        </form>
                    </div>
                </div>
                
            </div>
            <% } %>
    </div>
</body>

<footer>
    <%- include("../partials/footer.ejs") %>
</footer>

</html>